enum SocketIOEventKeys {
  // A new client has connected to the socket
  Connection = 'connection',

  // A client has disconnected from the socket
  Disconnect = 'disconnect',
}

export default SocketIOEventKeys;
